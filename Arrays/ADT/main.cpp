#include <iostream>
#include "array.hpp"

/* int main()
{
    int length = 5;
    Array list1 = Array(length, true, true);
    Array list2 = Array(length, true, true);

    for(int i = 0; i < length; i++)
    {
        // int value;
        // std::cout << std::endl << "Enter value for element in position " << i + 1 << ": ";
        //std::cin >> value;
        list1.insert(10 + i);
    }
    for(int i = 0; i < length; i++)
    {
        // int value;
        // std::cout << std::endl << "Enter value for element in position " << i + 1 << ": ";
        //std::cin >> value;
        list2.insert(7 + i);
    }

    // list1.display('\n', std::string("Before:"));
    
    // list1.insert(2,99);
    // list1.display('\n', std::string("\nAfer:"));
    // std::cout << "\nMax: " << list1.getMax();        
    
    //list1.reverse();
    // list1.display('\n', std::string("\nAfer:"));
    // std::cout << std::boolalpha << "\nisSorted: " << list2.isSorted();    
    Array mergedArray = list1.merge(list2, list1); 
    mergedArray.display('\t', std::string("\nMerged:"));
} */
